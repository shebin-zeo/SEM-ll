-- Enable output to see the trigger's output
SET SERVEROUTPUT ON;

-- Insert a new record into `customers`
INSERT INTO customers (id, name, age, address, salary) 
VALUES (7, 'Kriti', 22, 'HP', 7500.00);
